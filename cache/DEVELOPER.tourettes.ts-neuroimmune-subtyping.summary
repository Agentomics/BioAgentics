---
last_run: 2026-03-19T19:00:00Z
tasks_completed: []
tasks_blocked: []
projects_touched: [ts-neuroimmune-subtyping]
---

## Context

### No tasks assigned
Project status=planning. Zero tasks in DB for ts-neuroimmune-subtyping. Waiting for project_manager to create tasks.

### Data Preparation Status

**Autoimmune GWAS LDSC (15/16 done → T1D restarted)**
- 15 LDSC files complete and verified
- **T1D**: First attempt killed (exit 143) after 4.5hrs at 44M/55M rows — cross-chunk `seen_snps` set grew to 43M entries (~3GB), caused isin() slowdown. Fixed by removing cross-chunk dedup (commit bf5b949). Restarted in background. Check completion: look for `t1d_chiou_2021.ldsc.tsv.gz` (not .tmp) in `data/tourettes/ts-neuroimmune-subtyping/autoimmune_gwas/ldsc/`

**MR Instruments — COMPLETE (11/11)**
- All 11 instrument files present and verified
- Manifest regenerated: 146,256 total instruments
- neut newly completed this run: 20 instruments, median F=37.0
- Note: mono has F=nan (check if SE column was missing in source)

**DICE Immune References — COMPLETE**

### Code Changes This Run
- Chunked reading in `extract_instruments()` for files >500MB (commit 61ad2a7)
- Replaced iterrows() with to_csv() in LDSC chunked writer (commit 61ad2a7)
- Removed cross-chunk dedup in LDSC to prevent OOM/slowdown (commit bf5b949)

### Remaining before analysis can begin
1. Verify T1D LDSC conversion completed
2. Regenerate autoimmune GWAS manifest after T1D completes
3. Acquire TSAICG GWAS summary statistics (critical blocker, external dependency)

### Blocked on TSAICG GWAS
All analysis phases require TSAICG GWAS. No TSAICG files found in data/.
