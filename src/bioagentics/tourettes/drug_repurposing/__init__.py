"""Network pharmacology drug repurposing pipeline for Tourette syndrome.

Required packages:
    - networkx          (graph construction, shortest paths, module detection)
    - pandas            (data manipulation)
    - requests          (API queries: STRING, ChEMBL, ClinicalTrials.gov, LINCS)
    - matplotlib        (network and pathway visualizations)
    - python-louvain    (community detection via Louvain clustering)
    - numpy             (numerical operations, z-score computation)
    - scipy             (statistical tests, distance metrics)
"""
